Under the usloss extract the files and go to src once there open terminal in the dir and run "make install".
Once that it will install all the libraries for the usloss.
Confirm that everything is ready if you can see the file "libusloss.a" if it is there then it is complete.
